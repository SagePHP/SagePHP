# imports:
#     - { resource: VCS/di.yml }
#     - { resource: System/di.yml }

services:

    exec:
        class: SagePHP\System\exec

    git:
        class: SagePHP\VCS\git
        arguments: [@exec]
